{
  "name": "permission-matrix-builder",
  "version": "1.0.0",
  "description": "Reusable RBAC middleware with centralized permission matrix",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "test:watch": "node --import tsx --test --watch tests/**/*.test.ts",
    "start": "node dist/index.js"
  },
  "keywords": ["rbac", "authorization", "middleware", "permissions"],
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
